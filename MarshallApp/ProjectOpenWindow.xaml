<Window x:Class="MarshallApp.ProjectOpenWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="ProjectOpenWindow" Height="450" Width="800" WindowStyle="None">
    <Grid Background="{StaticResource PanelsBrush2}">
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        
        <Grid Grid.Row="0" MouseLeftButtonDown="UIElement_OnMouseLeftButtonDown">
            <Border x:Name="TopBorder" Grid.ColumnSpan="5" >
                <Border.Effect>
                    <BlurEffect Radius="1" RenderingBias="Performance"/>
                </Border.Effect>
                <Grid>
                    <StackPanel Orientation="Horizontal" Background="{StaticResource AdditionalBrush2}">
                        <TextBlock x:Name="Log" 
                                   Text="Open Project"
                                   Foreground="{StaticResource PrimaryTextBrush}" 
                                   FontSize="13" 
                                   FontWeight="SemiBold"
                                   Margin="5"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,5,0">
                        <Button x:Name="Exit" Width="25" Height="25" Margin="1" Style="{StaticResource FlatButtonStyle}" Click="Close_Click">
                            <Path Data="M14.5,13.5 L1.5,0.5 M1.5,13.5 L14.5,0.5" 
                                  Stroke="#CCFFFFFF" 
                                  StrokeThickness="2.3" 
                                  StrokeLineJoin="Round"
                                  Stretch="Uniform"
                                  Width="14" Height="14"/>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
        
        <Grid Grid.Row="1" >
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="35"/>
            </Grid.RowDefinitions>

            <ListBox x:Name="ProjectsList" Background="{StaticResource PanelsBrush}"
                     Foreground="{StaticResource PrimaryTextBrush}"
                     Margin="5"
                     HorizontalContentAlignment="Stretch"
                     Style="{StaticResource ModernListBoxStyle}"
                     ItemContainerStyle="{StaticResource ModernListBoxItemStyle}"
                     ScrollViewer.VerticalScrollBarVisibility="Auto"
                     ScrollViewer.HorizontalScrollBarVisibility="Hidden"/>

            <Border Grid.Row="1" Margin="0,0,5,5" BorderThickness="0,1,0,0" BorderBrush="{StaticResource SidebarButtonActiveBackground}">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Border Margin="2" Background="Blue" CornerRadius="5">
                        <Button Content="Open" Width="80" Click="Open_Click" Margin="2" Style="{StaticResource FlatButtonStyle}"/>
                    </Border>
                    <Button Content="Browse..." Width="80" Click="Browse_Click" Margin="2" Style="{StaticResource FlatButtonStyle}"/>
                    <Button Content="Cancel" Width="80" Click="Cancel_Click" Margin="2" Style="{StaticResource FlatButtonStyle}"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Window>
