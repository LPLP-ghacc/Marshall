<Window x:Class="MarshallApp.ProjectCreationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="ProjectCreationWindow" Height="450" Width="800" WindowStyle="None">
    <Grid Background="{StaticResource PanelsBrush2}">
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        
        <Grid Grid.Row="0" MouseLeftButtonDown="UIElement_OnMouseLeftButtonDown">
            <Border x:Name="TopBorder" Grid.ColumnSpan="5" >
                <Border.Effect>
                    <BlurEffect Radius="1" RenderingBias="Performance"/>
                </Border.Effect>
                <Grid>
                    <StackPanel Orientation="Horizontal" Background="{StaticResource AdditionalBrush2}">
                        <TextBlock x:Name="Log" 
                                   Text="New Project"
                                   Foreground="{StaticResource PrimaryTextBrush}" 
                                   FontSize="13" 
                                   FontWeight="SemiBold"
                                   Margin="5"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,5,0">
                        <Button x:Name="Exit" Width="25" Height="25" Margin="1" Style="{StaticResource FlatButtonStyle}" Click="Close_Click">
                            <Path Data="M14.5,13.5 L1.5,0.5 M1.5,13.5 L14.5,0.5" 
                                  Stroke="#CCFFFFFF" 
                                  StrokeThickness="2.3" 
                                  StrokeLineJoin="Round"
                                  Stretch="Uniform"
                                  Width="14" Height="14"/>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
        <Grid Row="1">
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition Height="35"/>
            </Grid.RowDefinitions>
            <Grid Grid.Row="0" Margin="10" HorizontalAlignment="Left">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <Grid Column="0">
                    <StackPanel Orientation="Vertical">
                        <TextBlock Text="Project Name:"  Height="28" Padding="5" Style="{StaticResource FlatTextBlockStyle}" Margin="5"/>
                        <TextBlock Text="Project Location:" Height="28" Padding="5" Margin="5" Style="{StaticResource FlatTextBlockStyle}"/>
                    </StackPanel>
                </Grid>
                <Grid Column="1">
                    <StackPanel Orientation="Vertical">
                        <TextBox x:Name="ProjectNameBox" Margin="5" HorizontalAlignment="Left" Height="28" Width="280" VerticalContentAlignment="Center" Style="{StaticResource FlatTextBoxStyle}"/>
                        <DockPanel Margin="5">
                            <TextBox x:Name="LocationBox" Height="28" Width="280" VerticalContentAlignment="Center" Style="{StaticResource FlatTextBoxStyle}"/>
                            <Button x:Name="BrowseButton" Content="Browse" Width="80" Margin="10,0,0,0" Click="BrowseButton_Click" Style="{StaticResource FlatButtonStyle}"/>
                        </DockPanel>
                        <TextBlock x:Name="FinalPathLabel" Margin="0,10,0,0" Style="{StaticResource FlatTextBlockStyle}"/>
                    </StackPanel>
                </Grid>
            </Grid>

            <Border Grid.Row="1" Margin="0,0,5,5" BorderThickness="0,1,0,0" BorderBrush="{StaticResource SidebarButtonActiveBackground}">
                <StackPanel  Orientation="Horizontal" HorizontalAlignment="Right">
                    <Border Margin="2" Background="Blue" CornerRadius="5">
                        <Button Content="Create" Width="90" Click="Create_Click" Margin="2" Style="{StaticResource FlatButtonStyle}"/>
                    </Border>
                    <Button Content="Browse..." Width="100" Click="Browse_Click" Margin="2" Style="{StaticResource FlatButtonStyle}"/>
                    <Button Content="Cancel" Width="90" Click="Cancel_Click" Margin="2" Style="{StaticResource FlatButtonStyle}"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Window>
