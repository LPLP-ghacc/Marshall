<Window x:Class="MarshallApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MarshallApp"
        mc:Ignorable="d" Height="800" Width="1200" ResizeMode="CanResizeWithGrip" WindowStyle="ToolWindow" Background="{x:Null}" Foreground="#FFBDBDBD" Title="Marshall">
    <Window.Resources>
        <Style TargetType="ResizeGrip">
            <Setter Property="Background" Value="DarkRed"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ResizeGrip">
                        <Grid Background="DarkRed"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid x:Name="RootGrid">
        <Grid.ColumnDefinitions>
            <ColumnDefinition x:Name="LeftCol" Width="200" MaxWidth="300"/>
            <ColumnDefinition Width="8"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="8"/>
            <ColumnDefinition x:Name="RightCol" Width="300" MaxWidth="600"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <StackPanel Grid.ColumnSpan="5" Orientation="Horizontal" Background="#222">
            <Button x:Name="AddButton" Content="Menu"
        Style="{StaticResource FlatButtonStyle}"
        Click="AddButton_Click"
        Margin="5,0">
                <Button.ContextMenu>
                    <ContextMenu Style="{StaticResource DarkContextMenuStyle}">
                        <MenuItem Header="Add block"
                      Click="AddBlock_Click"
                      Style="{StaticResource DarkMenuItemStyle}"/>
                        <Separator/>
                        <MenuItem Header="Settings"
                      Click="Settings_Click"
                      Style="{StaticResource DarkMenuItemStyle}"/>
                    </ContextMenu>
                </Button.ContextMenu>
            </Button>

            <Button x:Name="WindowButton" Content="Window"
                    Style="{StaticResource FlatButtonStyle}"
                    Click="WindowButton_Click"
                    Margin="5,0">
                <Button.ContextMenu>
                    <ContextMenu x:Name="WindowContextMenu" 
                                 Style="{StaticResource DarkContextMenuStyle}"
                                 PlacementTarget="{Binding ElementName=WindowButton}"
                                 StaysOpen="False">

                        <MenuItem x:Name="ScriptBrowserHideChecker" Header="Script Browser"
                                  Style="{StaticResource DarkMenuItemStyle}"
                                  IsCheckable="True"
                                  IsChecked="{Binding LeftPanelVisible, RelativeSource={RelativeSource AncestorType=Window}}" Checked="ScriptBrowserHideChecker_Checked"/>

                        <MenuItem x:Name="ScriptEditorHideChecker" Header="Code Editor"
                                  Style="{StaticResource DarkMenuItemStyle}"
                                  IsCheckable="True"
                                  IsChecked="{Binding RightPanelVisible, RelativeSource={RelativeSource AncestorType=Window}}" Checked="ScriptEditorHideChecker_Checked"/>
                    </ContextMenu>
                </Button.ContextMenu>
            </Button>
        </StackPanel>

        <local:ScriptBrowserPanel x:Name="ScriptBrowser" Grid.Row="1" Grid.Column="0" 
                              ScriptSelected="ScriptBrowser_ScriptSelected"
                              Visibility="{Binding LeftPanelVisible, Converter={StaticResource BoolToVis}}"/>
        
        <GridSplitter Grid.Row="1" Grid.Column="1" Width="8" Background="#333" HorizontalAlignment="Stretch"/>

        <Grid x:Name="MStackPanel" Grid.Row="1" Grid.Column="2" Background="#111"/>

        <GridSplitter Grid.Row="1" Grid.Column="3" Width="8" Background="#333" HorizontalAlignment="Stretch"/>

        <local:CodeEditorPanel x:Name="CodeEditor" Grid.Row="1" Grid.Column="4"/>
    </Grid>
</Window>